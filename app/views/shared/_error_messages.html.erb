<% if object.errors.any? %>

<div id="errors">
	<p><%= pluralize(object.errors.count, "problem") %> occurred while processing the following fields:</p>
	<ul>
		<% object.errors.full_messages.each do |message| %>
			<li><%= message %></li>
		<% end %>
	</ul>
</div>

<% end %>