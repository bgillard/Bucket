<%= form_for(@micropost) do |f| %>

	<div class="microform">
		<%= render 'shared/error_messages', :object => f.object %>
		<%= f.text_area :content %>
	</div>
	<br />
	<div class="actions">
		<%= f.submit "Submit!" %>
	</div>

<% end %>