<h1>Browse Users</h1>

<%= will_paginate %>

<div class="users">
	<% @users.each do |user| %>
		<div class="client">
			<%= gravatar_for user, :size => 30 %>
			<%= link_to user.name, user %>
		</div>
	<% end %>
</div>

<%= will_paginate %>