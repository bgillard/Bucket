<div id="profile">
	<%= gravatar_for @user, :size => 150 %>
	<br />
	<h1><%= @user.name %></h1>
	<p>Goals: <%= @user.microposts.count %>
	<p>URL: <%= link_to user_path(@user), @user %></p>
</div>

<div id="bucket">
	<h2>These are my goals!</h2>
	<% if @user.microposts.any? %>
		<%= render @microposts %>
	<% end %>
</div>